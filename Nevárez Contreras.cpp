//Librerias que se van a utilizar.
#include <stdlib.h> //Librería estándar para realizar funciones del sistema
#include <stdio.h.> //Librería estándar para la entrada y salida de información en pantalla

//Función principal
int main() { //Inicio del programa

	//Estructura del programa
		printf("Yo creo que el sentido de la vida es algo que inventamos nosotros porque como humanos con razonamiento abstracto ");
		printf("necesitamos entender, asimilar, encontrarle un sentido o finalidad, ");
		printf("para justificar nuestra existencia. ");
		printf("Yo le llamo asi, tu llamalo como quieras; ");
		printf("pero lo que no va a cambiar ");
		printf("es que, lo que de verdad importa es el camino.  ");
		system("pause");
		return 0;
} 
